// If you need middleware for protected routes, implement your own authentication check
export function middleware(request) {
    // Implement your route protection logic here
}

export const config = {
    matcher: [
        // Add your protected route patterns here
    ],
}